"""URL routes for `categories`. Mounted under `/api/v1/categories/`."""
from __future__ import annotations

from django.urls import path

app_name = "categories"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
