"""URL routes for `security`. Mounted under `/api/v1/security/`."""
from __future__ import annotations

from django.urls import path

app_name = "security"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
