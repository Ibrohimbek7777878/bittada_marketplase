"""URL routes for `escrow`. Mounted under `/api/v1/escrow/`."""
from __future__ import annotations

from django.urls import path

app_name = "escrow"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
