"""URL routes for `support`. Mounted under `/api/v1/support/`."""
from __future__ import annotations

from django.urls import path

app_name = "support"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
