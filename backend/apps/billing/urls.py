"""URL routes for `billing`. Mounted under `/api/v1/billing/`."""
from __future__ import annotations

from django.urls import path

app_name = "billing"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
