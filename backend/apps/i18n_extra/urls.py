"""URL routes for `i18n_extra`. Mounted under `/api/v1/i18n_extra/`."""
from __future__ import annotations

from django.urls import path

app_name = "i18n_extra"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
