"""URL routes for `blacklist`. Mounted under `/api/v1/blacklist/`."""
from __future__ import annotations

from django.urls import path

app_name = "blacklist"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
