"""URL routes for `notifications`. Mounted under `/api/v1/notifications/`."""
from __future__ import annotations

from django.urls import path

app_name = "notifications"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
