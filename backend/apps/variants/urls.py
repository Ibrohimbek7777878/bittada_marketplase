"""URL routes for `variants`. Mounted under `/api/v1/variants/`."""
from __future__ import annotations

from django.urls import path

app_name = "variants"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
