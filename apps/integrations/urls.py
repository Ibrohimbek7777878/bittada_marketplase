"""URL routes for `integrations`. Mounted under `/api/v1/integrations/`."""
from __future__ import annotations

from django.urls import path

app_name = "integrations"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
