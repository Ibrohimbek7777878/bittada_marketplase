"""URL routes for `seo`. Mounted under `/api/v1/seo/`."""
from __future__ import annotations

from django.urls import path

app_name = "seo"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
