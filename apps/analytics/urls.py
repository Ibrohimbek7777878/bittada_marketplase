"""URL routes for `analytics`. Mounted under `/api/v1/analytics/`."""
from __future__ import annotations

from django.urls import path

app_name = "analytics"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
