"""URL routes for `warehouse`. Mounted under `/api/v1/warehouse/`."""
from __future__ import annotations

from django.urls import path

app_name = "warehouse"

urlpatterns: list = [
    # path("", views.SomeView.as_view(), name="some"),
]
